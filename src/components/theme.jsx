import { createMuiTheme } from "material-ui/styles";

const myTheme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: "#444444"
    }
  },
  secondary: {
    main: "#ffffff"
  }
});

export default myTheme;
